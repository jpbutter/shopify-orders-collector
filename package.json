{
  "name": "shopify-orders-collector",
  "version": "0.1.0",
  "description": "TypeScript Shopify GraphQL order collector",
  "type": "module",
  "private": false,
  "license": "MIT",
  "engines": {"node": ">=20"},
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test tests/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
