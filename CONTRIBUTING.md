# Contributing

Use Node.js 20 or newer. Run **npm run typecheck** and **npm test**. Keep queries narrow and document any new personal data. Tests must use fictional stores and orders. Never commit API tokens, app secrets, webhook secrets or real exports.